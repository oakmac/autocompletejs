before launch checklist:
> MIT header on all files
> full docs for methods
> full docs for config options
> docs for shorthand / expansion
> example for every method
> example for every config option
> themes
> make sure the code conforms to JSLint or Google Closure style guide
> have a "cacheOptionsInLocalStorage" option on list config!
> have a "clearCache" option on the widget config
> ability to pick dates

Examples:
> localStorage
> AJAX using POST
> AJAX with postprocessing function
> simple usage
> simple usage with groups
> multiple lists
> example with optionHTML
> example with tokenHTML
> example with parent / child optionHTML
> example with parent / child tokenHTML
> allowFreeform
> simple AJAX
> AJAX with multiple lists
> AJAX with local options
> local lists mixed with AJAX
> AJAX using POST
> localStorage example
> extending the jq AJAX object